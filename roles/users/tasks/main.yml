    # - name: Download Java JRE
    #   get_url:
    #     url: http://download.oracle.com/otn-pub/java/jdk/8u60-b27/jre-8u60-linux-x64.rpm
    #     dest: /opt/jre-8u60-linux-x64.rpm
    #     headers: 'Cookie: gpw_e24=http%3A%2F%2Fwww.oracle.com%2F; oraclelicense=accept-securebackup-cookie'

    # - name: install Java JRE rpm from a local file
    #   apt:
    #     name: /opt/jre-8u60-linux-x64.rpm
    #     state: present
        
    # - name: Download Java JDK
    #   get_url:
    #     url: http://download.oracle.com/otn-pub/java/jdk/8u60-b27/jdk-8u60-linux-x64.rpm
    #     dest: /opt/jdk-8u60-linux-x64.rpm
    #     headers: 'Cookie: gpw_e24=http%3A%2F%2Fwww.oracle.com%2F; oraclelicense=accept-securebackup-cookie'

    # - name: Install Java JDK rpm from a local file
    #   apt:
    #     name: /opt/jdk-8u60-linux-x64.rpm
    #     state: present   

    - name: Install latest version of "openjdk-8-jre-headless" ignoring "install-recommends"
      apt:
        name: openjdk-8-jre-headless
        state: latest
        install_recommends: no
    
    - name: Maven Installation 
      apt:
        name: maven
        update_cache: yes
        state: present

    - name: Add user todoapp
      user:
        name: todoapp
        shell: /bin/bash 

    - name: Create dir
      file:
       path: /home/todoapp/users 
       state: directory 

    - name: Download users from nexus
      get_url:
        url: http://172.31.52.12:8081/repository/users/users-{{APP_VERSION}}.zip
        dest: /tmp/users.zip
        username: admin
        password: admin123
 
    - name: Unzip users Component
      unarchive:
        src: /tmp/users.zip
        dest: /home/todoapp/users
        remote_src: yes   

    - name: Copy systemd service file
      template:
        src: systemd.service
        dest: /etc/systemd/system/users.service             

    - name: Start users Service
      systemd:
        name: users
        daemon_reload: yes
        state: started
        enabled: yes
