- name: Demo on loop
  hosts: all
  tasks:
    - name: Print weeknames
      debug:
        msg: "week = {{ day }}"
      loop:
        - Monday
        - Tuesday
        - Wednesday
        - Thursday